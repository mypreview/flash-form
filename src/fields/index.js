/**
 * Internal dependencies
 */
import { blocks } from './shared';
import { registerBlockTypes } from './utils';

/**
 * Function to register inner-blocks provided by this plugin.
 */
registerBlockTypes( blocks );
